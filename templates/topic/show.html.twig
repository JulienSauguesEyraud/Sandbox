{% extends 'base.html.twig' %}

{% block body %}
    <h1>Sujet : {{ topic.title }}</h1>

    <h2>Commentaires</h2>

    <ul>
    {% for comment in comments %}
        <li>
            <p>{{ comment.description }}</p>
            <a href="{{ path('comment_show', {'topicId': topic.id, 'commentId': comment.id}) }}">
                Voir les réponses
            </a>
        </li>
    {% endfor %}
    </ul>

    <a href="{{ path('app_root') }}">Accueil</a>
{% endblock %}
